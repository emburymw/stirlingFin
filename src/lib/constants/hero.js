import handshakeImage from '../../assets/handshake.jpg'

/** Inline style for hero sections using the handshake photo background */
export const handshakeHeroStyle = {
  '--hero-image': `url(${handshakeImage})`,
}
